package Utils;

import Fields.Users;

public class cardValidation {

	public static boolean validateBank (Users users , int cardID) {
		
		boolean returnBankControl = true; // girilen cardID'nin banka sistemine gönderilip kontrol dönüþünün alýnmasý simule edilmiþtir.
		 
		if (returnBankControl=true)
			return true;
			else
				return false;
		
	};
	
}
